apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-add-task
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: {{.Release.Name}}-add-task
  ports:
  - port: 80
    targetPort: 80
